// Global
const HOME = "/";

// API
const USERS = "/api/users";
const USERS_URLs = {
    TEST: "/test",
    REGISTER: "/register"
}
const PROFILE = "/api/profile";
const POSTS = "/api/posts";

const routes = {
  HOME,
  USERS,
  USERS_URLs,
  PROFILE,
  POSTS
};

export default routes;
// Global
const HOME = "/";
const TEST = "/test";

// API
const USERS = "/api/users";
const PROFILE = "/api/profile";
const POSTS = "/api/posts";

const routes = {
  HOME,
  TEST,
  USERS,
  PROFILE,
  POSTS
};

export default routes;